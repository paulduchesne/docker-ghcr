name: testing-ci

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: print something ${{ github.repository }} and ${{ github.actor }}
        run: echo "hallo"
